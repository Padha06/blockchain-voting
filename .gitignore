# secrets — NEVER commit
.env
*.key
*.pem

# deps
node_modules/
web/node_modules/
web/.next/
web/out/

# hardhat
cache/
artifacts/
typechain-types/
coverage/
coverage.json

# census drafts (large, generated)
web/public/census/*.json
!web/public/census/.gitkeep

# generated run artifacts (local-only addresses/receipts)
deployment.json
web/.env.local.appchain
web/demo-receipts.json

# OS
.DS_Store
Thumbs.db

.vercel
